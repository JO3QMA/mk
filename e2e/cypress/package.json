{
  "name": "mk-go-e2e-cypress",
  "private": true,
  "version": "0.0.0",
  "description": "Cypress wrapper for running Misskey upstream e2e specs against mk-go backend",
  "scripts": {
    "cy:open": "cypress open --e2e --browser electron",
    "cy:run": "cypress run --e2e --browser electron"
  },
  "devDependencies": {
    "cypress": "15.11.0",
    "typescript": "5.5.4",
    "@types/node": "20.12.7"
  }
}
